[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "neuroscope-m1"
version = "0.3.0"
description = "NeuroScope (M1): dual-engine (local/OpenAI) mental health triage demo"
requires-python = ">=3.9"
